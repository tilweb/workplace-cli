---
name: Workplace CLI
description: "Download, install, and run Workplace CLI (Adacor)"
author: Adacor

inputs:
  prompt:
    description: The prompt to pass to the agent
    required: true
    default: |
      You are a helpful assistant
  ADACOR_AI_API_KEY:
    description: API Key for Adacor AI
    required: true
  install_python:
    description: |
      Whether or not to install Python
    required: true
    default: "true"
  python_version:
    description: |
      Version of Python to install. Warning: Unsupported.
    required: false

runs:
  using: "composite"
  steps:
    - name: Install Required Python Version
      if: inputs.install_python == true
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
      with:
        python-version-file: ${{ github.action_path }}/.python-version

    - name: Install Requested Python Version
      if: inputs.install_python && inputs.python_version
      uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
      with:
        python-version: ${{ inputs.python_version }}

    - name: Install uv
      uses: astral-sh/setup-uv@1e862dfacbd1d6d858c55d9b792c756523627244 # v7

    - name: Install Workplace CLI
      shell: bash
      working-directory: ${{ github.action_path }}
      run: |
        uv sync --locked --all-extras --dev

    - name: Run Workplace CLI
      id: run-workplace-cli
      shell: bash
      working-directory: ${{ github.action_path }}
      env:
        ADACOR_AI_API_KEY: "${{ inputs.ADACOR_AI_API_KEY }}"
        PROMPT_INPUT: "${{ inputs.prompt }}"
      run: |
        cd "${{ github.workspace }}"
        uv run --directory "${{ github.action_path }}" workplace \
          -p "$PROMPT_INPUT"
